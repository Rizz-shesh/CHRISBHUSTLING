# Environment
NODE_ENV=development
WEBSITE_URL=http://localhost:3000
APPLICATION_ID=

# Better Auth - Configuration
BETTER_AUTH_SECRET=

# Database (Supabase Postgres) — use the pooled "Transaction" connection
# string from Project Settings -> Database -> Connection string -> URI.
DATABASE_URL=

# Admin lead inbox
ADMIN_USERNAME=
ADMIN_PASSWORD=

# GoHighLevel inbound workflow webhook. Service inquiries POST here automatically.
GHL_WEBHOOK_URL=

# GoHighLevel inbound webhook for the site's exit-intent / delayed opt-in popup
# (separate from GHL_WEBHOOK_URL above — its own GHL Workflow -> Trigger:
# Inbound Webhook, since the payload shape/tag differs from service inquiries).
GHL_OPTIN_WEBHOOK_URL=

# Gateway Configuration
AI_GATEWAY_BASE_URL=
AI_GATEWAY_API_KEY=

# Payments and tracking
AUTUMN_SECRET_KEY=
